{
  "name": "@jingtang/infra",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "format": "terraform -chdir=tencent/saas fmt -recursive",
    "format:check": "terraform -chdir=tencent/saas fmt -check -recursive",
    "validate": "terraform -chdir=tencent/saas init -backend=false -input=false && terraform -chdir=tencent/saas validate && terraform -chdir=tencent/saas fmt -check -recursive"
  }
}
